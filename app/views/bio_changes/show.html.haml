.page-header
  = link_to bio_changes_path, :class => 'btn btn-default' do
    %span.glyphicon.glyphicon-list-alt
    Back
  = link_to edit_bio_change_path(@bio_change), :class => 'btn btn-primary' do
    %span.glyphicon.glyphicon-pencil
    Edit
  %h1 Show bio_change

%dl.dl-horizontal
  %dt User:
  %dd= @bio_change.user
  %dt Content:
  %dd= @bio_change.content
